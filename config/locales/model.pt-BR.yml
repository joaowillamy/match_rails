pt-BR:
  activerecord:
    models:
      user: Cliente
      lawyer: Advogado
      request: Pedido
    attributes:
      user:
        login: "Entrar"
      # will translate User attribute "login" as "Handle"
